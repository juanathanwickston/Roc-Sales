# Nixpacks Configuration
# Only override the install command to use npm ci for deterministic installs.
# Do not set a build command - this app has no build step (vanilla JS, no bundler).
# Start command is set in railway.json.

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci"]
